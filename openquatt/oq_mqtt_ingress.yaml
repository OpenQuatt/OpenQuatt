# ==============================================================================
# OpenQuatt - MQTT Ingress
# ==============================================================================
#
# Goal:
#   Accept optional external input values over MQTT.
#
# Scope:
#   - Uses a small OpenQuatt-specific ESP-IDF MQTT client, not ESPHome's global
#     MQTT component.
#   - No MQTT discovery, entity export, logging, telemetry snapshots or publish
#     profiles are generated.
#   - Subscribe topics are fixed by substitution and shown read-only in the web
#     app through /mqtt/status.
#   - Current inputs: cooling dew point, outside temperature, room temperature,
#     room setpoint, heating enable and cooling enable.
#   - Future inputs should follow the same openquatt/${device_name}/input/...
#     topic convention.
#
# ==============================================================================

openquatt_mqtt_config:
  id: oq_mqtt_config
  bootstrap_broker: "${mqtt_bootstrap_broker}"
  bootstrap_port: ${mqtt_bootstrap_port}
  bootstrap_username: "${mqtt_bootstrap_username}"
  bootstrap_password: "${mqtt_bootstrap_password}"
  dew_point_topic: "${mqtt_cooling_dew_point_topic}"
  dew_point_stale: ${mqtt_cooling_dew_point_stale_s}s
  dew_point_sensor: mqtt_cooling_dew_point
  dew_point_age_sensor: mqtt_cooling_dew_point_age
  dew_point_valid_binary_sensor: mqtt_cooling_dew_point_valid
  outside_temperature_topic: "${mqtt_outside_temperature_topic}"
  outside_temperature_stale: ${mqtt_outside_temperature_stale_s}s
  outside_temperature_sensor: mqtt_outside_temperature
  outside_temperature_age_sensor: mqtt_outside_temperature_age
  outside_temperature_valid_binary_sensor: mqtt_outside_temperature_valid
  room_temperature_topic: "${mqtt_room_temperature_topic}"
  room_temperature_stale: ${mqtt_room_temperature_stale_s}s
  room_temperature_sensor: mqtt_room_temperature
  room_temperature_age_sensor: mqtt_room_temperature_age
  room_temperature_valid_binary_sensor: mqtt_room_temperature_valid
  room_setpoint_topic: "${mqtt_room_setpoint_topic}"
  room_setpoint_stale: ${mqtt_room_setpoint_stale_s}s
  room_setpoint_sensor: mqtt_room_setpoint
  room_setpoint_age_sensor: mqtt_room_setpoint_age
  room_setpoint_valid_binary_sensor: mqtt_room_setpoint_valid
  heating_enable_topic: "${mqtt_heating_enable_topic}"
  heating_enable_stale: ${mqtt_heating_enable_stale_s}s
  heating_enable_binary_sensor: mqtt_heating_enable
  heating_enable_age_sensor: mqtt_heating_enable_age
  heating_enable_valid_binary_sensor: mqtt_heating_enable_valid
  cooling_enable_topic: "${mqtt_cooling_enable_topic}"
  cooling_enable_stale: ${mqtt_cooling_enable_stale_s}s
  cooling_enable_binary_sensor: mqtt_cooling_enable
  cooling_enable_age_sensor: mqtt_cooling_enable_age
  cooling_enable_valid_binary_sensor: mqtt_cooling_enable_valid
  default_enabled: ${mqtt_default_enabled}

globals:
  - id: api_input_cooling_dew_point_last_valid_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

  - id: api_input_outside_temperature_last_valid_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

  - id: api_input_room_temperature_last_valid_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

  - id: api_input_room_setpoint_last_valid_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

  - id: api_input_heating_enable_last_valid_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

  - id: api_input_cooling_enable_last_valid_ms
    type: uint32_t
    restore_value: no
    initial_value: "0"

  - id: api_input_cooling_dew_point_has_value
    type: bool
    restore_value: no
    initial_value: "false"

  - id: api_input_outside_temperature_has_value
    type: bool
    restore_value: no
    initial_value: "false"

  - id: api_input_room_temperature_has_value
    type: bool
    restore_value: no
    initial_value: "false"

  - id: api_input_room_setpoint_has_value
    type: bool
    restore_value: no
    initial_value: "false"

  - id: api_input_heating_enable_has_value
    type: bool
    restore_value: no
    initial_value: "false"

  - id: api_input_cooling_enable_has_value
    type: bool
    restore_value: no
    initial_value: "false"

number:
  - platform: template
    id: api_input_cooling_dew_point
    name: "api_input_cooling_dew_point"
    icon: mdi:water-thermometer
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    entity_category: config
    min_value: -20
    max_value: 35
    step: 0.1
    restore_value: false
    optimistic: true
    mode: box
    on_value:
      - lambda: |-
          id(api_input_cooling_dew_point_has_value) = true;
          id(api_input_cooling_dew_point_last_valid_ms) = (uint32_t) millis();

  - platform: template
    id: api_input_outside_temperature
    name: "api_input_outside_temperature"
    icon: mdi:thermometer
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    entity_category: config
    min_value: -40
    max_value: 60
    step: 0.1
    restore_value: false
    optimistic: true
    mode: box
    on_value:
      - lambda: |-
          id(api_input_outside_temperature_has_value) = true;
          id(api_input_outside_temperature_last_valid_ms) = (uint32_t) millis();

  - platform: template
    id: api_input_room_temperature
    name: "api_input_room_temperature"
    icon: mdi:home-thermometer
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    entity_category: config
    min_value: 0
    max_value: 50
    step: 0.1
    restore_value: false
    optimistic: true
    mode: box
    on_value:
      - lambda: |-
          id(api_input_room_temperature_has_value) = true;
          id(api_input_room_temperature_last_valid_ms) = (uint32_t) millis();

  - platform: template
    id: api_input_room_setpoint
    name: "api_input_room_setpoint"
    icon: mdi:thermostat
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    entity_category: config
    min_value: 5
    max_value: 35
    step: 0.1
    restore_value: false
    optimistic: true
    mode: box
    on_value:
      - lambda: |-
          id(api_input_room_setpoint_has_value) = true;
          id(api_input_room_setpoint_last_valid_ms) = (uint32_t) millis();

switch:
  - platform: template
    id: api_input_heating_enable
    name: "api_input_heating_enable"
    icon: mdi:radiator
    disabled_by_default: true
    entity_category: config
    restore_mode: RESTORE_DEFAULT_OFF
    optimistic: true
    turn_on_action:
      - lambda: |-
          id(api_input_heating_enable_has_value) = true;
          id(api_input_heating_enable_last_valid_ms) = (uint32_t) millis();
    turn_off_action:
      - lambda: |-
          id(api_input_heating_enable_has_value) = true;
          id(api_input_heating_enable_last_valid_ms) = (uint32_t) millis();

  - platform: template
    id: api_input_cooling_enable
    name: "api_input_cooling_enable"
    icon: mdi:snowflake-check
    disabled_by_default: true
    entity_category: config
    restore_mode: RESTORE_DEFAULT_OFF
    optimistic: true
    turn_on_action:
      - lambda: |-
          id(api_input_cooling_enable_has_value) = true;
          id(api_input_cooling_enable_last_valid_ms) = (uint32_t) millis();
    turn_off_action:
      - lambda: |-
          id(api_input_cooling_enable_has_value) = true;
          id(api_input_cooling_enable_last_valid_ms) = (uint32_t) millis();

sensor:
  - platform: template
    id: mqtt_cooling_dew_point
    name: "MQTT Cooling Dew Point"
    icon: mdi:water-thermometer
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    state_class: measurement
    accuracy_decimals: 2
    update_interval: never

  - platform: template
    id: mqtt_cooling_dew_point_age
    name: "MQTT Cooling Dew Point Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: mqtt_outside_temperature
    name: "MQTT Outside Temperature"
    icon: mdi:thermometer
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    state_class: measurement
    accuracy_decimals: 2
    update_interval: never

  - platform: template
    id: mqtt_outside_temperature_age
    name: "MQTT Outside Temperature Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: mqtt_room_temperature
    name: "MQTT Room Temperature"
    icon: mdi:home-thermometer
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    state_class: measurement
    accuracy_decimals: 2
    update_interval: never

  - platform: template
    id: mqtt_room_temperature_age
    name: "MQTT Room Temperature Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: mqtt_room_setpoint
    name: "MQTT Room Setpoint"
    icon: mdi:thermostat
    disabled_by_default: true
    unit_of_measurement: "°C"
    device_class: temperature
    state_class: measurement
    accuracy_decimals: 2
    update_interval: never

  - platform: template
    id: mqtt_room_setpoint_age
    name: "MQTT Room Setpoint Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: mqtt_heating_enable_age
    name: "MQTT Heating Enable Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: mqtt_cooling_enable_age
    name: "MQTT Cooling Enable Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: api_input_cooling_dew_point_age
    name: "API Input Cooling Dew Point Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: api_input_outside_temperature_age
    name: "API Input Outside Temperature Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: api_input_room_temperature_age
    name: "API Input Room Temperature Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: api_input_room_setpoint_age
    name: "API Input Room Setpoint Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: api_input_heating_enable_age
    name: "API Input Heating Enable Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

  - platform: template
    id: api_input_cooling_enable_age
    name: "API Input Cooling Enable Age"
    icon: mdi:timer-outline
    internal: true
    unit_of_measurement: "s"
    accuracy_decimals: 0
    update_interval: never

binary_sensor:
  - platform: template
    id: mqtt_cooling_dew_point_valid
    name: "MQTT Cooling Dew Point Valid"
    icon: mdi:water-check
    internal: true
    device_class: connectivity

  - platform: template
    id: mqtt_outside_temperature_valid
    name: "MQTT Outside Temperature Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: mqtt_room_temperature_valid
    name: "MQTT Room Temperature Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: mqtt_room_setpoint_valid
    name: "MQTT Room Setpoint Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: mqtt_heating_enable
    name: "MQTT Heating Enable"
    icon: mdi:radiator
    disabled_by_default: true
    device_class: heat

  - platform: template
    id: mqtt_heating_enable_valid
    name: "MQTT Heating Enable Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: mqtt_cooling_enable
    name: "MQTT Cooling Enable"
    icon: mdi:snowflake-check
    disabled_by_default: true
    device_class: cold

  - platform: template
    id: mqtt_cooling_enable_valid
    name: "MQTT Cooling Enable Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: api_input_cooling_dew_point_valid
    name: "API Input Cooling Dew Point Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: api_input_outside_temperature_valid
    name: "API Input Outside Temperature Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: api_input_room_temperature_valid
    name: "API Input Room Temperature Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: api_input_room_setpoint_valid
    name: "API Input Room Setpoint Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: api_input_heating_enable_valid
    name: "API Input Heating Enable Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

  - platform: template
    id: api_input_cooling_enable_valid
    name: "API Input Cooling Enable Valid"
    icon: mdi:check-decagram
    internal: true
    device_class: connectivity

interval:
  - interval: 5s
    then:
      - lambda: |-
          const uint32_t now_ms = (uint32_t) millis();

          auto update_numeric = [&](bool has_value,
                                    uint32_t last_valid_ms,
                                    uint32_t stale_s,
                                    bool entity_has_state,
                                    sensor::Sensor *age_sensor,
                                    binary_sensor::BinarySensor *valid_sensor) {
            // Recompute age/validity exactly like the MQTT ingress model.
            if (!has_value || !entity_has_state || last_valid_ms == 0U) {
              age_sensor->publish_state(NAN);
              valid_sensor->publish_state(false);
              return;
            }
            const uint32_t age_ms = (uint32_t) (now_ms - last_valid_ms);
            age_sensor->publish_state((float) age_ms / 1000.0f);
            const bool valid = stale_s == 0U || age_ms <= (stale_s * 1000UL);
            valid_sensor->publish_state(valid);
          };

          auto update_binary = [&](bool has_value,
                                   uint32_t last_valid_ms,
                                   uint32_t stale_s,
                                   sensor::Sensor *age_sensor,
                                   binary_sensor::BinarySensor *valid_sensor) {
            if (!has_value || last_valid_ms == 0U) {
              age_sensor->publish_state(NAN);
              valid_sensor->publish_state(false);
              return;
            }
            const uint32_t age_ms = (uint32_t) (now_ms - last_valid_ms);
            age_sensor->publish_state((float) age_ms / 1000.0f);
            const bool valid = stale_s == 0U || age_ms <= (stale_s * 1000UL);
            valid_sensor->publish_state(valid);
          };

          update_numeric(
              id(api_input_cooling_dew_point_has_value),
              id(api_input_cooling_dew_point_last_valid_ms),
              (uint32_t) ${api_input_cooling_dew_point_stale_s}UL,
              id(api_input_cooling_dew_point).has_state(),
              id(api_input_cooling_dew_point_age),
              id(api_input_cooling_dew_point_valid));
          update_numeric(
              id(api_input_outside_temperature_has_value),
              id(api_input_outside_temperature_last_valid_ms),
              (uint32_t) ${api_input_outside_temperature_stale_s}UL,
              id(api_input_outside_temperature).has_state(),
              id(api_input_outside_temperature_age),
              id(api_input_outside_temperature_valid));
          update_numeric(
              id(api_input_room_temperature_has_value),
              id(api_input_room_temperature_last_valid_ms),
              (uint32_t) ${api_input_room_temperature_stale_s}UL,
              id(api_input_room_temperature).has_state(),
              id(api_input_room_temperature_age),
              id(api_input_room_temperature_valid));
          update_numeric(
              id(api_input_room_setpoint_has_value),
              id(api_input_room_setpoint_last_valid_ms),
              (uint32_t) ${api_input_room_setpoint_stale_s}UL,
              id(api_input_room_setpoint).has_state(),
              id(api_input_room_setpoint_age),
              id(api_input_room_setpoint_valid));
          update_binary(
              id(api_input_heating_enable_has_value),
              id(api_input_heating_enable_last_valid_ms),
              (uint32_t) ${api_input_heating_enable_stale_s}UL,
              id(api_input_heating_enable_age),
              id(api_input_heating_enable_valid));
          update_binary(
              id(api_input_cooling_enable_has_value),
              id(api_input_cooling_enable_last_valid_ms),
              (uint32_t) ${api_input_cooling_enable_stale_s}UL,
              id(api_input_cooling_enable_age),
              id(api_input_cooling_enable_valid));
