# ==============================================================================
# OpenQuatt - Hardware Profile: Waveshare ESP32-S3-Relay-1CH
# ==============================================================================
substitutions:
  oq_hardware_profile: "waveshare"                     # Compile-time hardware profile id.
  esp_board: "esp32-s3-devkitc-1"                       # Active ESP32 board type.
  esp_flash_size: "16MB"                                # Active ESP flash size.
  esp_variant: "esp32s3"                                # Active ESP variant.
  esp_partitions: "${openquatt_root}/partitions/openquatt_16mb.csv" # Active partition table.
  uart_tx_pin: "GPIO17"                                 # Active RS485 TX pin.
  uart_rx_pin: "GPIO18"                                 # Active RS485 RX pin.
  uart_rts_pin: "GPIO21"                                # Active RS485 DE/RE (flow control) pin.
  ds18b20_pin: "GPIO2"                                  # Active DS18B20 1-Wire pin.
  oq_boiler_relay_pin: "GPIO47"                         # Active boiler relay output pin.
  web_auth_recovery_pin: "GPIO0"                        # Temporary recovery input, wired to a spare button/jumper for setup.

esphome:
  build_flags:
    - -DOQ_POWER_HOUSE_LEARNING_TARGET=1

packages:
  oq_power_house_learning: !include ../oq_power_house_learning.yaml
  oq_sensor_source_selects_no_opentherm: !include ../oq_sensor_source_selects_no_opentherm.yaml

psram:
  mode: octal
  speed: 80MHz
  ignore_not_found: false
