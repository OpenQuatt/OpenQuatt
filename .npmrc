# Dependency lifecycle scripts are disabled by default for this repository.
# OpenQuatt's web build uses packages directly after installation; do not bypass
# this without an explicit dependency-security review.
ignore-scripts=true
